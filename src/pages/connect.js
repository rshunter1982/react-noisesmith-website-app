import React from "react";

const Connect = () => {
    return (
        <div>
            <h1>
                Connect with Noisesmith
            </h1>
        </div>
    );
};

export default Connect;